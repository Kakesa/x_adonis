<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Personnes suivies par {{ user.name }}</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-black text-white">
    <div class="flex max-w-6xl mx-auto min-h-screen">
        @include('partials/sidebar')

        <main class="flex-grow border-l border-r border-gray-700 max-w-lg">
            <!-- Header -->
            <div class="sticky top-0 bg-black bg-opacity-70 backdrop-blur-md z-10 p-4 border-b border-gray-700">
                <div class="flex items-center gap-4">
                    <a href="{{ route('profile.show', { username: user.username }) }}" class="text-gray-400 hover:text-white">
                        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" /></svg>
                    </a>
                    <div>
                        <h1 class="text-xl font-bold">{{ user.name }}</h1>
                        <p class="text-gray-400 text-sm">@{{ user.username }}</p>
                    </div>
                </div>
                <!-- Onglets de navigation -->
                <nav class="flex mt-4 text-center text-gray-400 font-semibold">
                    <a href="{{ route('profile.followers', { username: user.username }) }}" class="flex-1 py-3 hover:bg-gray-800 transition">Abonnés</a>
                    <a href="{{ route('profile.following', { username: user.username }) }}" class="flex-1 py-3 text-white border-b-2 border-blue-500">Abonnements</a>
                </nav>
            </div>

            <!-- Liste des utilisateurs suivis -->
            <div>
                @each(person in list)
                    @include('partials/user_card', { user: person })
                @else
                    <p class="p-8 text-center text-gray-500">@{{ user.username }} ne suit personne pour le moment.</p>
                @end
            </div>
        </main>

        @include('partials/right-sidebar')
    </div>
</body>
</html>